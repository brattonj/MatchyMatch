# Deployment

The application is built with Vite and can be deployed to any static hosting service. The build command generates optimized production files in the dist directory. Environment configuration is handled through Vite's built-in mechanisms. The project includes a public directory with static assets like fonts and icons. GitHub Actions workflows automate testing and can be extended for deployment. The application is a single-page app that works with client-side routing.
