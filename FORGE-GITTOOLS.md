# Test Suite

This project uses Jest and React Testing Library for comprehensive testing, with tests organized in `src/__tests__/` and run via `npm test`, `npm run test:watch`, or `npm run test:coverage` commands.
The CI/CD pipeline automatically runs linting, tests, and builds on push and pull requests to the main and develop branches.
